# Database — PostgreSQL + pgvector
# For Docker Compose, use the service name as host:
#   DATABASE_URL=postgresql://postgres:postgres@db:5432/candidates
# For local dev (Postgres running on your machine):
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/candidates
