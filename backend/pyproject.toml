[project]
name = "candidate-search-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "psycopg2-binary",
    "pgvector",
    "sentence-transformers",
    "python-dotenv",
]
